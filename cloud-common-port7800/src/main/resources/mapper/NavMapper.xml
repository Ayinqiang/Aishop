<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.base.common.dao.NavMapper">

    <select id="SelectNavItemList" parameterType="integer" resultType="com.base.common.pojo.NavItem">
        SELECT * FROM router_data where belong = #{id}
    </select>

</mapper>
